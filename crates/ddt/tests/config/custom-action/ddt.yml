actions:
  - id: web-app
    stdin: false
    name: "Next.js dev server"
    steps:
      - name: "Run"
        run: |
          yarn dev

rules:
  - name: "Foo"
    if: ${{ matrix.foo == 1 }}
    steps:
      - name: "Remove previous files"
        run: rm -rf .next
