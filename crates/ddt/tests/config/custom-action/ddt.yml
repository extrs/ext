actions:
  - id: web-app
    name: "Next.js dev server"
    steps:
      - name: "Run"
        run: |
          yarn dev

rules:
  - name: "Foo"
    if: ${{ matrix.foo == 1 }}
    steps:
      - name: "Remove previous files"
        run: rm -rf .next
